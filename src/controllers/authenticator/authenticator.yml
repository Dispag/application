authenticator:
      name: authenticator
      handler: src/controllers/authenticator/authenticator-handler.login
      events:
          - http:
              method: post
              path: auth/login